{
  "name": "examples",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "server": "tsnd -r tsconfig-paths/register -r dotenv/config server",
    "client": "tsnd -r tsconfig-paths/register -r dotenv/config client",
    "create-sample-file": "tsnd -r dotenv/config examples/sample-file.ts",
    "clean": "rimraf ./upload/"
  },
  "dependencies": {
    "dotenv": "16.5.0",
    "express": "^5.1.0",
    "@uploadx/core": "^6.1.7"
  },
  "devDependencies": {
    "@types/express": "5.0.1",
    "ts-node-dev": "2.0.0"
  }
}
